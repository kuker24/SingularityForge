# v1.0.0 Linux Stable Readiness Report

Generated: 2026-07-09
Target: v1.0.0 - Stable Linux Runtime
Status: Pending final release gate at authoring time

## Local Verification Commands

```bash
npm run typecheck
npm run test:unit
npm run test:coverage
npm run verify:skills
npm run audit:skills
npm run token:report
npm run token:benchmark
node scripts/token-benchmark.mjs --profile all
npm run validate:adapters
npm run doctor
node scripts/doctor.mjs --json
node scripts/doctor.mjs --fix-permissions
npm run verify:installers
bash installer/install.sh --dry-run
bash installer/install-local.sh --dry-run "/tmp/singularityforge stable test"
```

## Expected Linux Local Result

All commands must exit 0 before tagging.

## CI Result Plan

After PR merge to `main`, verify:
```bash
gh run list --limit 6
```
Expected:
- CI: completed success
- Security: completed success

## Tag/Release Verification Plan

```bash
git ls-remote --tags origin v1.0.0
gh release view v1.0.0
```

## Remaining Non-Linux Limitations

- Windows remains Experimental.
- External integrations remain default OFF.
- Network-dependent scanners are not hard-blocking local gates.
